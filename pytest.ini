[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py

# 📁 Файлы с тестами (текущие и планируемые):
# - employees_tasks/tests/test_views.py         — тесты вьюсеттов (уже есть)
# - employees_tasks/tests/test_permissions.py   — права доступа (будет позже)
# - employees_tasks/tests/test_serializers.py   — сериализаторы (будет позже)
# - employees_tasks/tests/test_auth.py          — логин, токены (будет позже)
